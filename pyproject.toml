[project]
name = "gezimsejdiu-github-io"
version = "0.1.0"
description = "Personal website built with MkDocs Material"
requires-python = ">=3.10"
dependencies = [
  "mkdocs>=1.6",
  "mkdocs-material>=9.5",
  "mkdocs-minify-plugin>=0.8",
]
